# Entorno local — nunca commitear claves reales
.env
.env.local
.env.*.local

# Vercel
.vercel

# Backups de la base — nunca commitear datos de pacientes
backups/
*.sql
*.sql.gz

# Sistema operativo / editores
.DS_Store
Thumbs.db
*.log
node_modules/
